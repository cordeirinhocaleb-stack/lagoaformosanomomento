
import React from 'react';
import LayoutHeader from './layout/Header';

/**
 * ⚠️ ARQUIVO DEPRECADO / DEPRECATED FILE ⚠️
 * 
 * O Header ativo do site foi movido para: components/layout/Header.tsx
 * Por favor, faça suas edições no arquivo da pasta 'layout'.
 * 
 * The active Header has been moved to: components/layout/Header.tsx
 * Please edit the file in the 'layout' folder.
 */

const Header = (props: any) => {
  return <LayoutHeader {...props} />;
};

export default Header;
